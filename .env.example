# =============================================================================
# Dify Chat Docker 环境变量配置
# =============================================================================

# Docker 镜像配置
DOCKER_IMAGE=dify-chat:latest

# Nginx 端口配置
EXPOSE_NGINX_PORT=80
EXPOSE_NGINX_SSL_PORT=443
NGINX_PORT=80
NGINX_SSL_PORT=443

# Nginx 性能配置
NGINX_WORKER_PROCESSES=auto
NGINX_KEEPALIVE_TIMEOUT=65
NGINX_CLIENT_MAX_BODY_SIZE=10m

# =============================================================================
# React App API 配置 (运行时注入)
# =============================================================================

# 应用配置 API 基础路径
# 例如: http://your-domain.com/api/client
PUBLIC_APP_API_BASE=http://localhost:5300/api/client

# Dify 代理 API 基础路径
# 例如: http://your-domain.com/api/client/dify
PUBLIC_DIFY_PROXY_API_BASE=http://localhost:5300/api/client/dify

# =============================================================================
# Platform 后端配置
# =============================================================================

# Node.js 环境
NODE_ENV=production
PORT=5300

# 数据库配置
# 生产环境请使用 PostgreSQL 或 MySQL
# 示例: postgresql://username:password@hostname:port/database
# 示例: mysql://username:password@hostname:port/database
DATABASE_URL=file:./prod.db

# NextAuth 配置
# 请生成一个安全的随机密钥
# 可以使用: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here

# =============================================================================
# SSL 证书配置 (可选)
# =============================================================================

# SSL 证书配置
SSL_CERT_PATH=/etc/ssl/certs/server.crt
SSL_KEY_PATH=/etc/ssl/private/server.key

# Certbot 配置
CERTBOT_EMAIL=your-email@example.com
CERTBOT_DOMAINS=your-domain.com,www.your-domain.com

# =============================================================================
# 调试配置 (可选)
# =============================================================================

# 启用调试模式 (true/false)
PUBLIC_DEBUG_MODE=false

# 日志级别
LOG_LEVEL=info